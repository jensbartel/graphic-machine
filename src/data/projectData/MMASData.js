const mainView = `Left: Search Results. Right: Work View. Disclaimer: Images are from Google search and don't reflect the actual MMAS collection. All artwork data is fictional.<br><br><br>`; // note: the <br>s are a hack; remove

const searchMask = `Search mask.`;

const assistantEditor = `Assistant Editor view.`;

const MMASData = { mainView, searchMask, assistantEditor };

export default MMASData;
